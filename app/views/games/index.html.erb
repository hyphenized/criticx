<h1>Games</h1>
<%= link_to "New", new_game_path%>
<table>
  <thead>
    <th>Name</th>
    <th>Rating</th>
  </thead>
  <tbody>
    <% @games.each do |game| %>
    <tr>
      <td>
        <%= game.name %>
      </td>
      <td>
        <%= game.rating %>
      </td>
      <td>
        <%= link_to "Show", game_path(game) %>
      </td>
      <td>
        <%= link_to "Edit", edit_game_path(game) %>
      </td>
    </tr>
    <% end %>
  </tbody>
</table>
